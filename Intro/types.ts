// Types in typeScript

// -- Primitive types --
// Number  Null        Any
// String  Undefinded  Never
// Boolean Void        Unknown

// -- Object types --
// Object   Tuple
// Array    Enum
// Funtion  !Others

// Where do we apply TypeScript in out code?

// Variable Types
// - To assign a basic type to a we add :Type after the varible name
// ex
const movieTitle: string = "No country for old men";
const age: number = 34;
const isActive: boolean = true;

// ^ This is also call Type Annotation

// Strings
// Strings represent character values "This is a string value"
// We can tell TypeScript that something is a string using the
// type annotation of string (all lowercase)

// Numbers
// Some programming lauguages have many number types.
// flost, int etc.
// In typeScript (as well as JavaScript) numbers are just numbers.
// Numbers can be typed with a simple TYpe Annotation of number (all lowercase)

// Booleans
// Boolean variables represent simple true or false values
// Booleans can be types with simple type annotation of boolean (all lowercase)
